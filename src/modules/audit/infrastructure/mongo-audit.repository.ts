import { Injectable } from '@nestjs/common'; import { InjectModel } from '@nestjs/mongoose'; import { Model, Types } from 'mongoose'; import { AuditLog } from './audit-log.schema'; import { AuditRepository } from '../domain/audit.repository';
@Injectable() export class MongoAuditRepository implements AuditRepository { constructor(@InjectModel(AuditLog.name) private readonly model: Model<AuditLog>) {} async append(input: Parameters<AuditRepository['append']>[0]): Promise<void> { await this.model.create({ ...input, actorUserId: input.actorUserId ? new Types.ObjectId(input.actorUserId) : undefined, organizationId: input.organizationId ? new Types.ObjectId(input.organizationId) : undefined }); } }
