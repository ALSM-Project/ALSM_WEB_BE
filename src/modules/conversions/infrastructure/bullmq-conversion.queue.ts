import { Injectable, OnModuleDestroy } from '@nestjs/common'; import { ConfigService } from '@nestjs/config'; import { Queue } from 'bullmq'; import { ConversionPriority, ConversionQueuePort } from '../domain/conversion-job.types';
export const CONVERSION_QUEUE_NAME = 'conversion'; export const BULLMQ_PRIORITY: Record<ConversionPriority, number> = { [ConversionPriority.HIGH]: 1, [ConversionPriority.NORMAL]: 5, [ConversionPriority.LOW]: 10 };
@Injectable() export class BullMqConversionQueue implements ConversionQueuePort, OnModuleDestroy { private readonly queue: Queue<{ conversionJobId: string }>; constructor(config: ConfigService) { this.queue = new Queue(CONVERSION_QUEUE_NAME, { connection: { host: config.getOrThrow('REDIS_HOST'), port: config.getOrThrow<number>('REDIS_PORT'), password: config.get<string>('REDIS_PASSWORD') || undefined } }); } async enqueue(conversionJobId: string, priority: ConversionPriority): Promise<void> { await this.queue.add('execute-conversion', { conversionJobId }, { attempts: 3, backoff: { type: 'exponential', delay: 1000 }, priority: BULLMQ_PRIORITY[priority], removeOnComplete: 1000, removeOnFail: 5000 }); } async onModuleDestroy(): Promise<void> { await this.queue.close(); } }
