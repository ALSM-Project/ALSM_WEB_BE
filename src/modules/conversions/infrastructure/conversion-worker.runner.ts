import { Inject, Injectable, Logger, OnModuleDestroy } from '@nestjs/common'; import { ConfigService } from '@nestjs/config'; import { Worker } from 'bullmq'; import { CONVERSION_ENGINE, CONVERSION_JOB_REPOSITORY, ConversionEnginePort, ConversionJobRepository } from '../domain/conversion-job.types'; import { CONVERSION_QUEUE_NAME } from './bullmq-conversion.queue';
@Injectable() export class ConversionWorkerRunner implements OnModuleDestroy { private readonly logger = new Logger(ConversionWorkerRunner.name); private worker?: Worker<{ conversionJobId: string }>;
  constructor(private readonly config: ConfigService, @Inject(CONVERSION_JOB_REPOSITORY) private readonly jobs: ConversionJobRepository, @Inject(CONVERSION_ENGINE) private readonly engine: ConversionEnginePort) {}
  start(): void { if (!this.config.get<boolean>('CONVERSION_WORKER_ENABLED')) { this.logger.warn('Conversion worker is disabled. Set CONVERSION_WORKER_ENABLED=true only after configuring an external engine adapter.'); return; } this.worker = new Worker(CONVERSION_QUEUE_NAME, async (queueJob) => { const job = await this.jobs.markProcessing(queueJob.data.conversionJobId); if (!job) return; try { await this.engine.execute({ conversionJobId: job.id, projectId: job.projectId, inputReference: job.inputReference, conversionType: job.conversionType }); } catch (error) { const message = error instanceof Error ? error.message : 'External conversion engine failed'; await this.jobs.markFailed(job.id, 'CONVERSION_ENGINE_UNAVAILABLE', message); throw error; } }, { connection: { host: this.config.getOrThrow('REDIS_HOST'), port: this.config.getOrThrow<number>('REDIS_PORT'), password: this.config.get<string>('REDIS_PASSWORD') || undefined } }); this.logger.log('Conversion worker started.'); }
  async onModuleDestroy(): Promise<void> { await this.worker?.close(); }
}
